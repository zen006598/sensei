name: CD

on:
  push:
    branches: [main]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6.0.2

      - uses: astral-sh/setup-uv@v8.1.0
        with:
          enable-cache: true

      - name: Install dependencies
        run: uv sync --all-groups

      - name: Lint
        run: uv run ruff check src/ tests/

      - name: Format check
        run: uv run ruff format --check src/ tests/

      - name: Security audit
        run: uv run pip-audit

      - name: Tests
        run: uv run pytest -q

  build-push:
    needs: ci
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v6.0.2

      - uses: docker/login-action@v4.1.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@v7.1.0
        with:
          context: .
          push: true
          tags: ghcr.io/${{ github.repository }}:latest

  deploy:
    needs: build-push
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6.0.2

      - name: Connect Tailscale
        uses: tailscale/github-action@v4.1.2
        with:
          oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TS_OAUTH_CLIENT_SECRET }}
          tags: tag:ci

      - name: Copy docker-compose.yml
        uses: appleboy/scp-action@v1.0.0
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 2222
          source: docker-compose.yml
          target: ~/sensei

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.2.5
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          ANKIWEB_EMAIL: ${{ secrets.ANKIWEB_EMAIL }}
          ANKIWEB_PASSWORD: ${{ secrets.ANKIWEB_PASSWORD }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          ALLOWED_USER_IDS: ${{ secrets.ALLOWED_USER_IDS }}
          ANKI_DATA_PATH: ${{ secrets.ANKI_DATA_PATH }}
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 2222
          envs: TELEGRAM_TOKEN,ANKIWEB_EMAIL,ANKIWEB_PASSWORD,GEMINI_API_KEY,ALLOWED_USER_IDS,ANKI_DATA_PATH
          script: |
            mkdir -p ~/sensei
            sudo mkdir -p "$ANKI_DATA_PATH"
            sudo chmod 777 "$ANKI_DATA_PATH"
            {
              printf 'TELEGRAM_TOKEN=%s\n' "$TELEGRAM_TOKEN"
              printf 'ANKIWEB_EMAIL=%s\n' "$ANKIWEB_EMAIL"
              printf 'ANKIWEB_PASSWORD=%s\n' "$ANKIWEB_PASSWORD"
              printf 'GEMINI_API_KEY=%s\n' "$GEMINI_API_KEY"
              printf 'ALLOWED_USER_IDS=%s\n' "$ALLOWED_USER_IDS"
              printf 'ANKI_DATA_PATH=%s\n' "$ANKI_DATA_PATH"
            } > ~/sensei/.env
            cd ~/sensei
            docker compose pull
            docker compose up -d
